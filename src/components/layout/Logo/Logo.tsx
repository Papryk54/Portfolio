import styles from "./Logo.module.scss";

const Logo = () => {
	return (
		<div className={styles.wrapper}>
			<div className={styles.first}>
				<p>Logo</p>
			</div>
		</div>
	);
};

export default Logo;
